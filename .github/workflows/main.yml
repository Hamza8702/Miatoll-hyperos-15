name: Miatoll Kernel Build (KVM + LLVM)

on:
  workflow_dispatch:

jobs:
  kernel-build:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout Source
        uses: actions/checkout@v4

      - name: Install Build Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y bc build-essential flex bison lld llvm libssl-dev libelf-dev \
          cpio wget curl git zip unzip tar clang lld ccache pahole

      - name: Setup ccache
        uses: actions/cache@v4
        with:
          path: /home/runner/.cache/ccache
          key: kernel-22.04-ccache-${{ github.sha }}
          restore-keys: |
            kernel-22.04-ccache-

      - name: Setup Toolchains
        run: |
          # Using Proton Clang - Reliable and self-contained LLVM suite
          git clone --depth=1 https://github.com/kdrag0n/proton-clang.git clang
          
          # GCC Toolchains for auxiliary compilation tasks
          git clone --depth=1 https://github.com/LineageOS/android_prebuilts_gcc_linux-x86_aarch64_aarch64-linux-android-4.9 gcc64
          git clone --depth=1 https://github.com/LineageOS/android_prebuilts_gcc_linux-x86_arm_arm-linux-androideabi-4.9 gcc32

      - name: Build Kernel
        run: |
          # ccache Setup
          export USE_CCACHE=1
          export CCACHE_EXEC=/usr/bin/ccache
          export CCACHE_DIR=/home/runner/.cache/ccache
          ccache -M 5G
          ccache -z

          # Environment Variables
          export ARCH=arm64
          export SUBARCH=arm64
          export KBUILD_BUILD_HOST=ubuntu-24-04-actions
          export KBUILD_BUILD_USER="Ayse"
          export PATH="${PWD}/clang/bin:${PWD}/gcc64/bin:${PWD}/gcc32/bin:${PATH}"
          
          # 1. Initialize Defconfig
          make O=out ARCH=arm64 vendor/xiaomi/kinetis_defconfig Ek.config
          
          # 2. Inject KVM Configurations
          ./scripts/config --file out/.config -e KVM
          ./scripts/config --file out/.config -e VIRTUALIZATION
          ./scripts/config --file out/.config -e KVM_ARM_HOST
          ./scripts/config --file out/.config -e KVM_ARM_PMU
          
          # Sync config with dependencies
          make O=out ARCH=arm64 olddefconfig

          # 3. Start Compilation with full LLVM toolset
          # LLVM=1 and LLVM_IAS=1 are crucial to avoid Ubuntu 24.04 host conflicts
          make -j$(nproc --all) O=out \
            ARCH=arm64 \
            CC="ccache clang" \
            LLVM=1 \
            LLVM_IAS=1 \
            CLANG_TRIPLE=aarch64-linux-gnu- \
            CROSS_COMPILE=aarch64-linux-android- \
            CROSS_COMPILE_ARM32=arm-linux-androideabi- \
            LD=ld.lld \
            AR=llvm-ar \
            NM=llvm-nm \
            OBJCOPY=llvm-objcopy \
            OBJDUMP=llvm-objdump \
            STRIP=llvm-strip \
            CONFIG_NO_ERROR_ON_MISMATCH=y
          
          ccache -s

      - name: Package with AnyKernel3
        run: |
          TIMESTAMP=$(date +"%Y%m%d-%H")
          # Check for built image (Proton Clang standard output)
          if [ -f out/arch/arm64/boot/Image.gz-dtb ]; then
            git clone --depth=1 https://github.com/osm0sis/AnyKernel3 AnyKernel
            cp out/arch/arm64/boot/Image.gz-dtb AnyKernel/
            [ -f out/arch/arm64/boot/dtb.img ] && cp out/arch/arm64/boot/dtb.img AnyKernel/
            [ -f out/arch/arm64/boot/dtbo.img ] && cp out/arch/arm64/boot/dtbo.img AnyKernel/
            cd AnyKernel
            # Adjusting device check for Miatoll
            sed -i 's/do.devicecheck=1/do.devicecheck=0/g' anykernel.sh
            zip -r9 ../Miatoll-KVM-Kernel-${TIMESTAMP}.zip *
            cd ..
          else
            echo "Build failed: Image.gz-dtb not found!"
            exit 1
          fi

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Miatoll-KVM-Kernel-Result
          path: ./*.zip
